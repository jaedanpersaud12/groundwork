{
  "$schema": "https://ui.shadcn.com/schema/registry-item.json",
  "name": "view-toggle",
  "title": "View toggle",
  "description": "Segmented control for swapping one view of the same data for another.",
  "files": [
    {
      "path": "registry/groundwork/ui/view-toggle.tsx",
      "content": "\"use client\";\n\nimport * as React from \"react\";\n\nimport { cn } from \"@/lib/utils\";\n\n/** Segmented control for swapping one view of the same data for another. */\nfunction ViewToggle<T extends string>({\n  value,\n  onChange,\n  options,\n  label,\n  className,\n}: {\n  value: T;\n  onChange: (value: T) => void;\n  options: readonly { value: T; label: string; icon?: React.ReactNode }[];\n  /** Accessible name for the group, e.g. \"Row density\". */\n  label?: string;\n  className?: string;\n}) {\n  return (\n    <div\n      role=\"group\"\n      aria-label={label}\n      data-slot=\"view-toggle\"\n      className={cn(\"inline-flex h-8 items-center rounded-lg border border-border p-0.5\", className)}\n    >\n      {options.map((option) => (\n        <button\n          key={option.value}\n          type=\"button\"\n          onClick={() => onChange(option.value)}\n          aria-pressed={value === option.value}\n          className={cn(\n            \"inline-flex h-full items-center gap-1.5 rounded-md px-2.5 text-xs font-medium outline-none transition-colors focus-visible:ring-2 focus-visible:ring-ring/50 [&_svg]:size-3.5\",\n            value === option.value ? \"bg-foreground text-background\" : \"text-muted-foreground hover:text-foreground\",\n          )}\n        >\n          {option.icon}\n          {option.label}\n        </button>\n      ))}\n    </div>\n  );\n}\n\nexport { ViewToggle };\n",
      "type": "registry:ui"
    }
  ],
  "meta": {
    "version": "1.0.0",
    "tier": "primitive",
    "track": "minor"
  },
  "type": "registry:ui"
}
