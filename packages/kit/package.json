{
  "name": "@ja3dan/kit",
  "version": "0.3.1",
  "description": "Set a project up on groundwork and keep it current: init, list, sync components with a 3-way merge, update skills, lock, check, doctor, and link to a local registry.",
  "license": "MIT",
  "type": "module",
  "bin": {
    "kit": "./dist/kit.js"
  },
  "files": [
    "dist"
  ],
  "engines": {
    "node": ">=20.18.1"
  },
  "scripts": {
    "build": "bun scripts/bundle-assets.ts && bun build src/cli.ts --target node --outfile dist/kit.js",
    "test": "bun test --timeout 60000",
    "typecheck": "tsc --noEmit",
    "prepublishOnly": "bun run build"
  },
  "dependencies": {
    "shadcn": "4.21.0",
    "@ja3dan/tokens": "workspace:*",
    "@ja3dan/eslint-plugin": "workspace:*",
    "@typescript-eslint/parser": "^8.70.0",
    "eslint": "^9"
  },
  "devDependencies": {
    "@types/node": "^20",
    "typescript": "^5"
  },
  "publishConfig": {
    "access": "public"
  }
}
